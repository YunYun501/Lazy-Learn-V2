/** Centralized backend connection config — single source of truth. */
export const BASE_URL = 'http://127.0.0.1:8000'
export const API_BASE = `${BASE_URL}/api`
export const HEALTH_URL = `${BASE_URL}/health`
